7.  **การนับจำนวนสมาชิกใน Array:**
    
    -   **โจทย์:** กำหนด Array ชนิด `char` ที่เก็บชื่อ "HELLO" (ใช้ `{ 'H', 'E', 'L', 'L', 'O', '\0' }`) แล้วหาว่า Array นี้มีกี่ตัวอักษร (ไม่นับ `\0`).
        
    -   **ตัวอย่าง:**
        
        ```
        Length of string: 5
        
        ```