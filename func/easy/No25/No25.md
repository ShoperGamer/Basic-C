**ข้อ 25: แปลงเซลเซียสเป็นฟาเรนไฮต์**
    
    -   สร้างฟังก์ชันชื่อ `celsiusToFahrenheit` ที่รับ `float celsius` เป็นพารามิเตอร์ แล้วส่งค่าอุณหภูมิในหน่วยฟาเรนไฮต์กลับไปเป็น `float` (`F = C * 9/5 + 32`)